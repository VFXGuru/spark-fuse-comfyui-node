[project]
name = "spark-fuse-comfyui-node"
version = "0.1.0"
description = "ComfyUI extension to render the current workflow on Spark Fuse cloud GPUs"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"
dependencies = [
    "spark-fuse-messenger>=0.3.0",
]

[tool.comfy]
PublisherId = "vfxguru"
DisplayName = "Spark Fuse Cloud GPU Bridge"
Repository = "https://github.com/VFXGuru/spark-fuse-comfyui-node"
