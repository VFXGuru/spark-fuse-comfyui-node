[project]
name = "spark-fuse-comfyui-node"
version = "0.1.0"
description = "ComfyUI extension to render the current workflow on Spark Fuse cloud GPUs"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "spark-fuse @ git+https://github.com/VFXGuru/spark-fuse-messenger.git",
]

[tool.comfy]
PublisherId = "vfxguru"
DisplayName = "Spark Fuse Bridge"
Repository = "https://github.com/VFXGuru/spark-fuse-comfyui-node"
